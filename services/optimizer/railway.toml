[build]
builder = "DOCKERFILE"
dockerfilePath = "services/optimizer/Dockerfile.railway"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
