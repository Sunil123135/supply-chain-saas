[build]
  # Workspace root — package-lock.json lives here (not in apps/web)
  base = "."
  command = "npm ci && npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
